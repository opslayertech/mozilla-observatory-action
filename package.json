{
	"name": "mozilla-observatory-action",
	"version": "1.0.0",
	"description": "An action to run the Mozilla Observatory scan and return results.",
	"type": "module",
	"main": "index.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "https://opslayer.com/",
	"license": "ISC",
	"dependencies": {
		"@actions/core": "^1.11.1"
	}
}
